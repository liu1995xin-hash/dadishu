# Arduino Mega 2560 开发板接线说明

本说明适合第一次接线的使用者。请先断开开发板的 USB 电源，全部接好并检查无误后再通电。

## 1. 一个开关怎样接

每个开关使用 `COM` 和 `NC` 两个接口，`NO` 接口不使用。

```text
开关 COM  ───── Arduino GND
开关 NC   ───── Arduino 数字针脚
开关 NO   ───── 不接
```

多个开关的 `COM` 可以共同连接到开发板的 `GND`。每个开关的 `NC` 则要连接到下表指定的数字针脚。

> 接线口诀：COM 接 GND，NC 接数字针脚，NO 留空。

## 2. 六组针脚

每组有 4 个开关。按动同一组中的任意一个开关，都会作为这一组被击中。

| 信号 | 这一组的 4 个 NC 接口分别连接 |
|---|---|
| 信号 1 | D2、D3、D4、D5 |
| 信号 2 | D6、D7、D8、D9 |
| 信号 3 | D10、D11、D12、D13 |
| 信号 4 | D22、D23、D24、D25 |
| 信号 5 | D26、D27、D28、D29 |
| 信号 6 | D30、D31、D32、D33 |

例如：信号 1 这一组的 4 个开关，COM 都接 GND，NC 分别接 D2、D3、D4、D5。

## 3. 接好后怎样检查

1. 检查所有开关均为 `COM + NC` 接法，`NO` 没有接线。
2. 检查信号线没有接到 `5V`，也没有互相短接。
3. 使用 USB 连接电脑，开发板程序的串口波特率设为 `115200`。
4. 不按任何开关时，串口应显示 `0 0 0 0 0 0`。
5. 按动某一组的开关时，对应位置应由 `0` 变为 `1`；松开后应恢复为 `0`。

如果一直保持按下超过 1 秒，开发板程序会暂时屏蔽该输入。松开开关、信号恢复为 `0` 后，下一次按动才会再次生效。

## 4. 常见接错情况

- 如果未按开关时显示 `1`，请先检查是否误接了 `NO`。
- 如果按动没有反应，请检查 COM 是否接到 GND、NC 是否接到正确的数字针脚。
- 桌面程序连接串口后仍无数据，请确认选择了 Arduino Mega 2560 对应的串口，并确认波特率为 `115200`。

