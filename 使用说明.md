# 药材打地鼠游戏使用说明

本说明面向学生和家长，介绍游戏的连接、设置和操作方法。

## 一、第一次使用

1. 将 Arduino Mega 2560 开发板连接到电脑。
2. 打开游戏程序。
3. 点击右上角“设置”。
4. 点击“刷新”，在“串口”下拉列表中选择开发板对应的 `COM` 端口。
5. 点击“连接”。状态文字显示“已连接”后，说明程序已经开始接收开发板信号。
6. 初次体验可保留默认配置：初始10分、胜利30分、生成间隔2秒、限时30秒、横2纵3。
7. 点击“开始游戏”，或按键盘空格键开始。

使用 Windows 单文件版时，只需双击 `药材打地鼠.exe`。六张药材图片和运行组件都已包含在 EXE 中，不需要安装 Python，也不需要另外复制“素材”文件夹。

如果使用 Python 源码启动，请先在项目文件夹中安装依赖并运行：

```powershell
py -m pip install -r requirements.txt
py mole_game.py
```

## 二、游戏界面

- 左上角显示剩余时间。
- “开始游戏”用于开始新的一局。
- “结束游戏”用于提前结束当前游戏。
- 游戏进行中显示当前分数。
- 中间的白色格子是药材出现和命中的区域。
- 右上角“设置”用于展开或收起全部配置项。

## 三、设置项说明

### 1. 串口、刷新和连接

| 项目 | 作用 | 操作结果 |
|---|---|---|
| 串口 | 选择 Arduino 在电脑上的通信端口 | 选错端口时，程序无法收到开发板信号 |
| 刷新 | 重新查找当前可用串口 | 插入或重新连接开发板后，应点击一次 |
| 连接 | 打开所选串口并开始读取六位信号 | 连接成功后，按钮变为“断开” |
| 断开 | 停止读取当前串口 | 断开后，实体开发板不能控制游戏 |

连接本程序前，请关闭 Arduino IDE 的“串口监视器”，因为同一个串口通常不能同时被两个程序占用。

### 2. 初始分数

- 可选10～100分，每次增加10分，默认10分。
- 每局开始时，当前分数从这里设置的数值开始。
- 初始分数越高，距离胜利分数越近；但初始分数必须小于胜利分数，否则游戏不会开始。

### 3. 胜利分数

- 可选30～200分，每次增加10分，默认30分。
- 游戏分数达到或超过该数值时，立即判定“游戏胜利”。
- 分数越高，通常需要命中更多正确药材才能胜利。

### 4. 生成间隔

- 可选0.5～5.0秒，每次增加0.5秒，默认2.0秒。
- 表示程序每隔多长时间尝试在空白格中增加一个药材目标。
- 时间越短，药材出现得越快，游戏越忙；时间越长，游戏节奏越慢。
- 如果每个格子都已经显示目标或处于命中特效中，本次生成会自动跳过。

### 5. 限时时长

- 可选20、30、40、50或60秒，默认30秒。
- 点击开始后，左上角从所选时间开始倒计时。
- 倒计时到0时，程序立即停止本局、清除全部药材，并显示“游戏结束”。

### 6. 界面布局

可选择以下三种布局：

| 布局 | 格子数量 | 默认信号顺序（从左上向右、再到下一行） |
|---|---:|---|
| 横2纵3 | 6格 | 第6、5 / 第4、3 / 第2、1位 |
| 横3纵2 | 6格 | 第6、5、4 / 第3、2、1位 |
| 横2纵2 | 4格 | 第6、5 / 第4、3位 |

- 选择布局后，游戏区域会立即改变，不需要另外点击保存。
- 切换布局时，信号映射会恢复为上表中的默认顺序。
- 程序只记住最新选择的布局和最新保存的映射，不分别保存三种布局的历史设置。
- 横2纵2默认不使用第1、2位信号。这两位仍会被读取，但不会触发格子。

### 7. 格子信号设置

该设置决定开发板的每一位信号控制哪个格子。

1. 点击“格子信号设置”展开列表。
2. 从“第1行第1列”开始，为每个格子选择一位信号。
3. 信号选项后会直接注明对应的 Arduino 针脚。
4. 全部格子设置完成后，点击“保存信号配置”。
5. 只有所有格子都有信号且没有重复时，配置才会保存并正式生效。

六位信号与针脚的对应关系如下：

| 串口信号 | Arduino 针脚 |
|---|---|
| 第1位信号 | D2、D3、D4、D5 |
| 第2位信号 | D6、D7、D8、D9 |
| 第3位信号 | D10、D11、D12、D13 |
| 第4位信号 | D22、D23、D24、D25 |
| 第5位信号 | D26、D27、D28、D29 |
| 第6位信号 | D30、D31、D32、D33 |

同一个信号不能同时分配给两个格子。如果新选择的信号已经被其他格子使用，原格子会自动变为“未配置”。例如，第1行第1列改为第5位信号，而第1行第2列原来使用第5位，那么第1行第2列会自动清空，需要重新选择后再保存。

修改下拉框但没有点击“保存信号配置”时，实体开发板仍按上一次成功保存的映射控制游戏。

### 8. 药材分数设置

点击“药材分数设置”后，可以分别调整每种药材的分值。

| 选项 | 命中结果 |
|---|---|
| `+1`～`+20` | 当前分数增加相应数值 |
| `-1`～`-20` | 当前分数减少相应数值 |
| `-999（直接结束游戏）` | 不进行普通加减分，立即判定游戏失败 |

不提供0分选项。默认规则为：

- 黄芩、丹参、青蒿、紫苏：每次命中加10分。
- 大麻叶：每次命中减5分。
- 罂粟花：命中后直接游戏失败。

药材分值在每局开始时锁定。本局进行中不能修改，结束后才能调整下一局的规则。

## 四、开始和进行游戏

1. 确认串口已经连接，并完成需要的设置。
2. 点击“开始游戏”或按空格键。
3. 游戏开始后会立即随机出现一个药材，之后按“生成间隔”继续尝试生成。
4. 每个药材最多显示4秒，未命中时会自动消失。
5. 使用实体装置击打相应位置，或用鼠标点击格子进行电脑端模拟。
6. 命中有药材的格子后，该目标只能结算一次。除直接失败外，格子中央会显示具体分值变化，例如“+10分”或“-5分”。
7. 药材会先放大0.3秒，再缩小0.3秒；动画结束后清除，并把分值计入总分。
8. 命中空白格不加分、不扣分，也不会显示特效。

开发板的同一位信号持续为1时不会连续命中。信号必须先恢复为0，下一次变为1才算新的命中。

## 五、游戏如何结束

| 结束方式 | 显示结果 | 后续操作 |
|---|---|---|
| 点击“结束游戏” | 游戏结束 | 再次点击开始或按空格键可开始新一局 |
| 游戏中按空格键 | 游戏结束 | 再按一次空格键可开始新一局 |
| 倒计时到0 | 游戏结束 | 点击开始或按空格键可开始新一局 |
| 分数达到胜利分数 | 游戏胜利 | 显示1秒后回到准备状态；期间按空格可提前跳过 |
| 分数小于等于0 | 游戏失败 | 显示1秒后回到准备状态；期间按空格可提前跳过 |
| 命中设置为-999的药材 | 游戏失败 | 与普通失败的等待和空格操作相同 |

游戏停止、胜利或失败后，所有药材会立即清除，后续串口信号不会改变分数。

## 六、配置记忆

程序会记住最近一次有效配置。下次打开时会自动恢复，无需每次重新设置。

Windows配置文件固定保存在：

```text
%LOCALAPPDATA%\MedicinalMoleGame\settings.json
```

移动游戏程序或以后使用打包后的程序，不会改变这个配置文件的位置。格子信号只有点击“保存信号配置”并通过校验后才会写入；其他下拉设置在选择后自动保存。

## 七、常见问题

### 找不到串口

确认开发板已经连接电脑，再点击“刷新”。如果仍然没有端口，请检查USB数据线和开发板供电。

### 点击连接后没有击打反应

先确认游戏已经开始。再检查串口是否选择正确、信号映射是否已点击保存，以及 Arduino IDE 的串口监视器是否已经关闭。

### 刚开始游戏时第一次击打没有反应

游戏开始后收到的第一帧只用于记录开发板当前状态。先让开关处于松开状态，再进行下一次完整击打。

### 某个格子由错误的位置控制

打开“格子信号设置”，根据行列位置和针脚注释重新选择信号，全部配置完整后点击“保存信号配置”。

### 信号配置不能保存

查看提示文字，确认每个格子都不是“未配置”。同一个信号不能重复使用；发生冲突时，原来占用该信号的格子会被自动清空。

### 没有开发板时如何测试

游戏进行中可以直接用鼠标点击格子。鼠标点击与该格收到串口命中的游戏处理相同，适合检查目标、动画、加减分和胜负流程。
